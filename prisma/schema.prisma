generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  TEACHER
  OTHER_STAFF
  PARENT
  COACH
  SUBSCRIBER
}

enum ActivityType {
  DIAPER_CHANGE
  NAP
  BOTTLE
  MEAL
  SNACK
  ACTIVITY
  TASK_CHECKLIST
  BEHAVIOR
  INCIDENT
  OTHER
}

enum ProgressStatus {
  NOT_STARTED
  IN_PROGRESS
  COMPLETED
  PASSED
  FAILED
}

enum OutboundEmailStatus {
  QUEUED
  SENDING
  SENT
  FAILED
  CANCELED
}

enum AuditAction {
  CREATE
  READ
  UPDATE
  DELETE
  LOGIN
  LOGOUT
  EXPORT
  EMAIL_SENT
}

enum ChecklistPeriod {
  DAY
  WEEK
  MONTH
}

enum MilestoneItemKind {
  POLICY
  PROCEDURE
  VIDEO
  LESSON
  OTHER
}

enum TeacherRecordType {
  CERTIFICATE
  ACHIEVEMENT
  EMPLOYEE_OF_THE_MONTH
  CAREER_LADDER
}

enum NotificationType {
  MESSAGE
  COMPLIANCE_ALERT
  ACTIVITY_UPDATE
  PROGRESS_UPDATE
  SYSTEM
  FORM_RENEWAL
}

enum ArchiveType {
  PROGRESS
  FULL_RECORD
}

enum BehaviorPlanStatus {
  ACTIVE
  COMPLETED
  ARCHIVED
}

enum ObservationType {
  CAMERA
  IN_CLASS
}

enum FollowUpType {
  PARENT
  CAMERA_OBSERVATION
  GENERAL
}

enum FollowUpStatus {
  OPEN
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum Priority {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum PermissionType {
  PHOTO_RELEASE
  FIELD_TRIP
  MEDICAL_TREATMENT
  TRANSPORTATION
  SUNSCREEN_APPLICATION
  WATER_ACTIVITIES
  OTHER
}

enum PermissionStatus {
  PENDING
  GRANTED
  DENIED
  REVOKED
}

enum BehaviorGoalStatus {
  NOT_STARTED
  IN_PROGRESS
  MET
  NOT_MET
}

enum StaffAttendanceStatus {
  PRESENT
  LATE
  ABSENT
  HALF_DAY
}

enum TimeOffStatus {
  PENDING
  APPROVED
  DENIED
  CANCELLED
}

enum EvaluationStatus {
  DRAFT
  SUBMITTED
  ACKNOWLEDGED
}

model User {
  id                 String              @id @default(uuid())
  email              String              @unique
  name               String?
  password           String?
  dob                DateTime?
  hireDate           DateTime?
  aboutMe            String?
  pictureUrl         String?
  role               Role                @default(PARENT)
  roles              Role[]              @default([])
  centers            CenterUser[]
  children           Child[]
  guardianships      ChildGuardian[]
  teacherClasses     TeacherClass[]
  recordedActivities ActivityLog[]
  threadParticipants ThreadParticipant[]
  sentMessages       Message[]
  formSubmissions    FormSubmission[]
  createdInvites     CenterInvite[]      @relation("InvitesCreated")
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  ProgressEntry      ProgressEntry[]
  AuditLog           AuditLog[]
  OutboundEmail      OutboundEmail[]
  milestoneChecklistCompletions MilestoneChecklistItemCompletion[]
  teacherRecords               TeacherRecord[] @relation("TeacherRecords")
  createdTeacherRecords        TeacherRecord[] @relation("TeacherRecordsCreated")
  notifications                Notification[]            @relation("NotificationsReceived")
  notificationPreferences      NotificationPreference[]  @relation("NotificationPreferences")
  behaviorPlansCreated         BehaviorPlan[]            @relation("BehaviorPlansCreated")
  performanceSnapshots         TeacherPerformanceSnapshot[] @relation("TeacherPerformanceSnapshots")
  staffAttendances             StaffAttendance[]         @relation("StaffAttendances")
  staffAttendancesRecorded     StaffAttendance[]         @relation("StaffAttendancesRecorded")
  timeOffRequests              TimeOffRequest[]          @relation("TimeOffRequests")
  timeOffReviewed              TimeOffRequest[]          @relation("TimeOffReviewed")
  trainingLogs                 TrainingLog[]             @relation("TrainingLogs")
  trainingLogsRecorded         TrainingLog[]             @relation("TrainingLogsRecorded")
  evaluationsReceived          TeacherEvaluation[]       @relation("EvaluationsReceived")
  evaluationsGiven             TeacherEvaluation[]       @relation("EvaluationsGiven")
  budgetExpensesRecorded       BudgetExpense[]           @relation("BudgetExpensesRecorded")
  permissionsGranted           ChildPermission[]         @relation("PermissionsGranted")
  eventsCreated                Event[]                   @relation("EventsCreated")
  shiftSchedules               ShiftSchedule[]           @relation("ShiftSchedules")
  assignedChecklistItems       MilestoneChecklistItem[]   @relation("ChecklistItemAssignee")
  coachObservationsGiven       CoachObservation[]         @relation("CoachObservationsGiven")
  coachObservationsReceived    CoachObservation[]         @relation("CoachObservationsReceived")
  coachFollowUpsCreated        CoachFollowUp[]            @relation("CoachFollowUpsCreated")
  coachFollowUpsAssigned       CoachFollowUp[]            @relation("CoachFollowUpsAssigned")
  dailyChecklistCompletions    DailyChecklistCompletion[] @relation("DailyChecklistCompletedBy")
  assignedDailyChecklists      DailyChecklist[]           @relation("DailyChecklistAssignedUser")
  dailyChecklistNotes          DailyChecklistNote[]       @relation("DailyChecklistNotesCreatedBy")
}

model Center {
  id               String           @id @default(uuid())
  name             String
  address          String?
  users            CenterUser[]
  classes          ClassRoom[]
  children         Child[]
  attendances      Attendance[]
  lessonCategories LessonCategory[]
  lessons          Lesson[]
  checklists       TaskChecklist[]
  subscription     Subscription?
  messageThreads   MessageThread[]
  formTemplates    FormTemplate[]
  policyDocuments  PolicyDocument[]
  invites          CenterInvite[]
  auditLogs        AuditLog[]
  outboundEmails   OutboundEmail[]
  milestoneChecklistPlans MilestoneChecklistPlan[]
  behaviorPlans    BehaviorPlan[]
  teacherPerformanceSnapshots TeacherPerformanceSnapshot[]
  staffAttendances StaffAttendance[]
  timeOffRequests  TimeOffRequest[]
  trainingLogs     TrainingLog[]
  classroomBudgets ClassroomBudget[]
  teacherEvaluations TeacherEvaluation[]
  events           Event[]
  shiftSchedules   ShiftSchedule[]
  affiliates       Affiliate[]
  coachObservations CoachObservation[]
  coachFollowUps   CoachFollowUp[]
  dailyChecklists  DailyChecklist[]
  createdAt        DateTime         @default(now())
}

model CenterUser {
  id       String @id @default(uuid())
  user     User   @relation(fields: [userId], references: [id])
  userId   String
  center   Center @relation(fields: [centerId], references: [id])
  centerId String
  role     Role

  @@unique([userId, centerId])
}

model ClassRoom {
  id       String         @id @default(uuid())
  name     String
  capacity Int?
  ageRange String?
  center   Center         @relation(fields: [centerId], references: [id])
  centerId String
  children Child[]
  attendances Attendance[]
  teachers TeacherClass[]
  budgets  ClassroomBudget[]
  coachObservations CoachObservation[]
  dailyChecklists   DailyChecklist[]
}

model TeacherClass {
  id        String    @id @default(uuid())
  teacher   User      @relation(fields: [teacherId], references: [id])
  teacherId String
  classRoom ClassRoom @relation(fields: [classId], references: [id])
  classId   String

  @@unique([teacherId, classId])
}

model Child {
  id              String           @id @default(uuid())
  firstName       String
  lastName        String?
  birthDate       DateTime?
  emergencyContact String?
  emergencyContacts Json?
  parentContacts   Json?
  allergies       String?
  healthAssessmentDocuments Json?
  enrollmentDocuments Json?
  iefDocuments    Json?
  immunizationDocuments Json?
  infantDocuments Json?
  otherDocuments  Json?
  feedingPlan     Json?
  center          Center           @relation(fields: [centerId], references: [id])
  centerId        String
  classRoom       ClassRoom?       @relation(fields: [classRoomId], references: [id])
  classRoomId     String?
  progress        Progress[]
  activities      ActivityLog[]
  attendances     Attendance[]
  completedTasks  ChildTask[]
  parent          User?            @relation(fields: [parentId], references: [id])
  parentId        String?
  guardians       ChildGuardian[]
  formSubmissions FormSubmission[]
  enrollmentStartDate DateTime?
  enrollmentEndDate   DateTime?
  permissions     ChildPermission[]
  createdAt       DateTime         @default(now())
  OutboundEmail   OutboundEmail[]
  milestoneChecklistCompletions MilestoneChecklistItemCompletion[]
  behaviorPlans   BehaviorPlan[]
}

model ChildGuardian {
  id              String   @id @default(uuid())
  child           Child    @relation(fields: [childId], references: [id], onDelete: Cascade)
  childId         String
  guardian        User     @relation(fields: [guardianId], references: [id], onDelete: Cascade)
  guardianId      String
  relationship    String?
  isPrimary       Boolean  @default(false)
  receivesUpdates Boolean  @default(true)
  createdAt       DateTime @default(now())

  @@unique([childId, guardianId])
  @@index([guardianId])
}

model LessonCategory {
  id          String   @id @default(uuid())
  center      Center   @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  name        String
  description String?
  kind        String   @default("PACKAGE")
  groupName   String?
  ageRange    String?
  imageUrl    String?
  videoUrl    String?
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  lessons Lesson[]

  @@index([centerId, name, ageRange])
  @@index([centerId, sortOrder])
}

model Lesson {
  id               String              @id @default(uuid())
  title            String
  description      String?
  subCategory      String?
  term             String?
  reference        String?
  center           Center              @relation(fields: [centerId], references: [id])
  centerId         String
  category         LessonCategory?     @relation(fields: [categoryId], references: [id], onDelete: SetNull)
  categoryId       String?
  policyDocument   PolicyDocument?     @relation(fields: [policyDocumentId], references: [id], onDelete: SetNull)
  policyDocumentId String?
  linkedLesson     Lesson?             @relation("LessonLinkedLesson", fields: [linkedLessonId], references: [id], onDelete: SetNull)
  linkedLessonId   String?
  linkedFrom       Lesson[]            @relation("LessonLinkedLesson")
  media            String[]            @default([])
  goals            LessonGoal[]
  progress         Progress[]
  remediationsFrom LessonRemediation[] @relation("LessonRemediationsFrom")
  remediationsTo   LessonRemediation[] @relation("LessonRemediationsTo")
  milestoneChecklistItems MilestoneChecklistItem[]
  behaviorPlanGoals BehaviorPlanGoal[]
  supplies         LessonSupply[]
  dailyChecklistItems DailyChecklistItem[]

  @@index([policyDocumentId])
  @@index([linkedLessonId])
  @@index([centerId, term])
}

model LessonGoal {
  id              String   @id @default(uuid())
  lesson          Lesson   @relation(fields: [lessonId], references: [id], onDelete: Cascade)
  lessonId        String
  goalIndex       Int
  title           String
  description     String?
  passingCriteria Json?
  active          Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  progress Progress[]
  milestoneChecklistItems MilestoneChecklistItem[]

  @@unique([lessonId, goalIndex])
  @@index([lessonId, active])
}

model LessonRemediation {
  id           String   @id @default(uuid())
  fromLesson   Lesson   @relation("LessonRemediationsFrom", fields: [fromLessonId], references: [id], onDelete: Cascade)
  fromLessonId String
  toLesson     Lesson   @relation("LessonRemediationsTo", fields: [toLessonId], references: [id], onDelete: Cascade)
  toLessonId   String
  reason       String?
  createdAt    DateTime @default(now())

  @@unique([fromLessonId, toLessonId])
  @@index([fromLessonId])
  @@index([toLessonId])
}

model Progress {
  id             String          @id @default(uuid())
  child          Child           @relation(fields: [childId], references: [id])
  childId        String
  lesson         Lesson          @relation(fields: [lessonId], references: [id])
  lessonId       String
  status         ProgressStatus  @default(NOT_STARTED)
  goalIndex      Int             @default(1)
  lessonGoal     LessonGoal?     @relation(fields: [lessonGoalId], references: [id], onDelete: SetNull)
  lessonGoalId   String?
  achievedAt     DateTime?
  nextGoals      Progress[]      @relation("GoalProgression")
  previousGoal   Progress?       @relation("GoalProgression", fields: [previousGoalId], references: [id])
  previousGoalId String?
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  entries        ProgressEntry[]

  @@unique([childId, lessonId, goalIndex])
  @@index([childId, status])
  @@index([lessonId, status])
}

model ProgressEntry {
  id           String         @id @default(uuid())
  progress     Progress       @relation(fields: [progressId], references: [id], onDelete: Cascade)
  progressId   String
  status       ProgressStatus
  notes        String?
  details      Json?
  media        String[]       @default([])
  recordedBy   User?          @relation(fields: [recordedById], references: [id], onDelete: SetNull)
  recordedById String?
  occurredAt   DateTime       @default(now())

  @@index([progressId, occurredAt])
  @@index([recordedById, occurredAt])
}

model ActivityLog {
  id           String       @id @default(uuid())
  child        Child        @relation(fields: [childId], references: [id])
  childId      String
  type         ActivityType @default(OTHER)
  details      Json?
  notes        String?
  recordedBy   User?        @relation(fields: [recordedById], references: [id])
  recordedById String?
  createdAt    DateTime     @default(now())
  isBackdated  Boolean      @default(false)
}

model Attendance {
  id           String    @id @default(uuid())
  day          DateTime
  center       Center    @relation(fields: [centerId], references: [id])
  centerId     String
  child        Child     @relation(fields: [childId], references: [id])
  childId      String
  classRoom    ClassRoom? @relation(fields: [classRoomId], references: [id])
  classRoomId  String?
  checkedInAt  DateTime?
  checkedOutAt DateTime?
  notes        String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@unique([childId, day])
  @@index([centerId, day])
  @@index([centerId, day, classRoomId])
}

model TaskChecklist {
  id          String   @id @default(uuid())
  title       String
  description String?
  center      Center   @relation(fields: [centerId], references: [id])
  centerId    String
  tasks       Task[]
  createdAt   DateTime @default(now())
}

model MilestoneChecklistPlan {
  id          String          @id @default(uuid())
  center      Center          @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  title       String
  description String?
  period      ChecklistPeriod
  periodStart DateTime
  active      Boolean         @default(true)
  items       MilestoneChecklistItem[]
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt

  @@unique([centerId, period, periodStart, title])
  @@index([centerId, period, periodStart])
}

model MilestoneChecklistItem {
  id               String                 @id @default(uuid())
  plan             MilestoneChecklistPlan @relation(fields: [planId], references: [id], onDelete: Cascade)
  planId           String
  sortOrder        Int                    @default(0)
  title            String
  kind             MilestoneItemKind      @default(OTHER)
  url              String?
  policyDocument   PolicyDocument?        @relation(fields: [policyDocumentId], references: [id], onDelete: SetNull)
  policyDocumentId String?
  lesson           Lesson?                @relation(fields: [lessonId], references: [id], onDelete: SetNull)
  lessonId         String?
  lessonGoal       LessonGoal?            @relation(fields: [lessonGoalId], references: [id], onDelete: SetNull)
  lessonGoalId     String?
  notes            String?
  priority         Priority?
  dueAt            DateTime?
  assignedToId     String?
  assignedTo       User?                  @relation("ChecklistItemAssignee", fields: [assignedToId], references: [id], onDelete: SetNull)
  completions      MilestoneChecklistItemCompletion[]
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt

  @@index([planId, sortOrder])
  @@index([lessonId])
  @@index([lessonGoalId])
  @@index([policyDocumentId])
  @@index([assignedToId])
  @@index([dueAt])
}

model MilestoneChecklistItemCompletion {
  id           String                 @id @default(uuid())
  item         MilestoneChecklistItem @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId       String
  child        Child                  @relation(fields: [childId], references: [id], onDelete: Cascade)
  childId      String
  completedAt  DateTime?
  recordedBy   User?                  @relation(fields: [recordedById], references: [id], onDelete: SetNull)
  recordedById String?
  createdAt    DateTime               @default(now())
  updatedAt    DateTime               @updatedAt

  @@unique([childId, itemId])
  @@index([childId, completedAt])
  @@index([itemId, completedAt])
}

model Task {
  id          String        @id @default(uuid())
  title       String
  checklist   TaskChecklist @relation(fields: [checklistId], references: [id])
  checklistId String
  policyLink  String?
  mediaLink   String?
  taskTime    String?
  completed   ChildTask[]
  createdAt   DateTime      @default(now())
}

model ChildTask {
  id          String    @id @default(uuid())
  child       Child     @relation(fields: [childId], references: [id])
  childId     String
  task        Task      @relation(fields: [taskId], references: [id])
  taskId      String
  completedAt DateTime?

  @@unique([childId, taskId])
}

model Subscription {
  id          String    @id @default(uuid())
  center      Center    @relation(fields: [centerId], references: [id])
  centerId    String    @unique
  tier        String
  active      Boolean   @default(true)
  expiresAt   DateTime?
  paymentInfo Json?
}

model CenterInvite {
  id          String    @id @default(uuid())
  code        String    @unique
  center      Center    @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  role        Role
  active      Boolean   @default(true)
  expiresAt   DateTime?
  createdBy   User?     @relation("InvitesCreated", fields: [createdById], references: [id])
  createdById String?
  createdAt   DateTime  @default(now())

  @@index([centerId])
}

model MessageThread {
  id        String   @id @default(uuid())
  center    Center?  @relation(fields: [centerId], references: [id])
  centerId  String?
  title     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  participants ThreadParticipant[]
  messages     Message[]
}

model ThreadParticipant {
  id        String        @id @default(uuid())
  thread    MessageThread @relation(fields: [threadId], references: [id], onDelete: Cascade)
  threadId  String
  user       User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId     String
  lastReadAt DateTime?
  createdAt  DateTime      @default(now())

  @@unique([threadId, userId])
}

model Message {
  id        String        @id @default(uuid())
  thread    MessageThread @relation(fields: [threadId], references: [id], onDelete: Cascade)
  threadId  String
  sender    User          @relation(fields: [senderId], references: [id], onDelete: Cascade)
  senderId  String
  body      String
  createdAt DateTime      @default(now())
}

model FormTemplate {
  id                String   @id @default(uuid())
  center            Center?  @relation(fields: [centerId], references: [id])
  centerId          String?
  title             String
  description       String?
  targetRole        Role
  schema            Json?
  active            Boolean  @default(true)
  requiresRenewal   Boolean  @default(false)
  renewalPeriodDays Int?
  autoFillMapping   Json?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  submissions FormSubmission[]
}

model FormSubmission {
  id            String       @id @default(uuid())
  template      FormTemplate @relation(fields: [templateId], references: [id], onDelete: Cascade)
  templateId    String
  submittedBy   User         @relation(fields: [submittedById], references: [id], onDelete: Cascade)
  submittedById String
  child         Child?       @relation(fields: [childId], references: [id])
  childId       String?
  data          Json?
  status        String       @default("SUBMITTED")
  expiresAt     DateTime?
  renewedById   String?
  appliedToChild Boolean     @default(false)
  appliedAt     DateTime?
  appliedById   String?
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt
  childPermissions ChildPermission[]
}

model PolicyDocument {
  id          String   @id @default(uuid())
  center      Center?  @relation(fields: [centerId], references: [id])
  centerId    String?
  title       String
  description String?
  url         String
  roles       Role[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @default(now()) @updatedAt
  milestoneChecklistItems MilestoneChecklistItem[]
  lessons     Lesson[]
  dailyChecklistItems DailyChecklistItem[]
}

model AuditLog {
  id         String      @id @default(uuid())
  center     Center?     @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId   String?
  actor      User?       @relation(fields: [actorId], references: [id], onDelete: SetNull)
  actorId    String?
  action     AuditAction
  entityType String
  entityId   String?
  ip         String?
  userAgent  String?
  metadata   Json?
  createdAt  DateTime    @default(now())

  @@index([centerId, createdAt])
  @@index([actorId, createdAt])
  @@index([entityType, entityId])
}

model OutboundEmail {
  id          String              @id @default(uuid())
  center      Center?             @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String?
  child       Child?              @relation(fields: [childId], references: [id], onDelete: SetNull)
  childId     String?
  createdBy   User?               @relation(fields: [createdById], references: [id], onDelete: SetNull)
  createdById String?
  to          String
  cc          String[]            @default([])
  bcc         String[]            @default([])
  subject     String
  template    String?
  payload     Json?
  status      OutboundEmailStatus @default(QUEUED)
  scheduledAt DateTime            @default(now())
  sentAt      DateTime?
  error       String?
  createdAt   DateTime            @default(now())
  updatedAt   DateTime            @updatedAt

  @@index([status, scheduledAt])
  @@index([centerId, createdAt])
  @@index([childId, createdAt])
}

model TeacherRecord {
  id          String            @id @default(uuid())
  teacher     User              @relation("TeacherRecords", fields: [teacherId], references: [id], onDelete: Cascade)
  teacherId   String
  type        TeacherRecordType
  title       String
  description String?
  date        DateTime          @default(now())
  fileUrl     String?
  fileName    String?
  createdBy   User?             @relation("TeacherRecordsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  createdById String?
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  @@index([teacherId, type])
  @@index([teacherId, date])
  @@index([createdById])
}

model ProgressArchive {
  id           String   @id @default(uuid())
  centerId     String
  childId      String
  childName    String
  schoolYear   String
  archivedAt   DateTime @default(now())
  archivedById String?
  data         Json

  @@index([centerId, schoolYear])
  @@index([childId, schoolYear])
}

model DataArchive {
  id           String      @id @default(uuid())
  centerId     String
  childId      String
  childName    String
  schoolYear   String
  archiveType  ArchiveType @default(FULL_RECORD)
  archivedAt   DateTime    @default(now())
  archivedById String?
  data         Json

  @@index([centerId, schoolYear])
  @@index([childId, schoolYear])
  @@index([archiveType, centerId])
}

model Notification {
  id          String           @id @default(uuid())
  recipient   User             @relation("NotificationsReceived", fields: [recipientId], references: [id], onDelete: Cascade)
  recipientId String
  type        NotificationType
  title       String
  body        String
  link        String?
  metadata    Json?
  read        Boolean          @default(false)
  createdAt   DateTime         @default(now())

  @@index([recipientId, read, createdAt])
}

model NotificationPreference {
  id      String           @id @default(uuid())
  user    User             @relation("NotificationPreferences", fields: [userId], references: [id], onDelete: Cascade)
  userId  String
  type    NotificationType
  enabled Boolean          @default(true)

  @@unique([userId, type])
}

model BehaviorPlan {
  id          String             @id @default(uuid())
  child       Child              @relation(fields: [childId], references: [id], onDelete: Cascade)
  childId     String
  center      Center             @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  title       String
  description String?
  status      BehaviorPlanStatus @default(ACTIVE)
  targetDomains String[]         @default([])
  goals       BehaviorPlanGoal[]
  createdBy   User?              @relation("BehaviorPlansCreated", fields: [createdById], references: [id], onDelete: SetNull)
  createdById String?
  startDate   DateTime           @default(now())
  endDate     DateTime?
  reviewDate  DateTime?
  notes       String?
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@index([childId, status])
  @@index([centerId, status])
  @@index([createdById])
}

model BehaviorPlanGoal {
  id             String             @id @default(uuid())
  plan           BehaviorPlan       @relation(fields: [planId], references: [id], onDelete: Cascade)
  planId         String
  sortOrder      Int                @default(0)
  domain         String
  title          String
  description    String?
  targetScore    Int?
  currentScore   Int?
  status         BehaviorGoalStatus @default(NOT_STARTED)
  strategies     String[]           @default([])
  lesson         Lesson?            @relation(fields: [lessonId], references: [id], onDelete: SetNull)
  lessonId       String?
  achievedAt     DateTime?
  notes          String?
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt

  @@index([planId, sortOrder])
  @@index([planId, status])
}

model TeacherPerformanceSnapshot {
  id               String   @id @default(uuid())
  teacher          User     @relation("TeacherPerformanceSnapshots", fields: [teacherId], references: [id], onDelete: Cascade)
  teacherId        String
  center           Center   @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId         String
  period           String
  periodType       String
  childrenCount    Int      @default(0)
  avgCompletionRate Float   @default(0)
  avgDomainScore   Float?
  goalsCompleted   Int      @default(0)
  goalsFailed      Int      @default(0)
  activityLogCount Int      @default(0)
  behaviorLogCount Int      @default(0)
  attendanceDays   Int      @default(0)
  compositeScore   Float    @default(0)
  breakdown        Json?
  computedAt       DateTime @default(now())

  @@unique([teacherId, centerId, period, periodType])
  @@index([centerId, period])
  @@index([teacherId, period])
}

model StaffAttendance {
  id           String               @id @default(uuid())
  user         User                 @relation("StaffAttendances", fields: [userId], references: [id], onDelete: Cascade)
  userId       String
  center       Center               @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId     String
  date         DateTime
  clockIn      DateTime?
  clockOut     DateTime?
  status       StaffAttendanceStatus @default(PRESENT)
  lateMinutes  Int                  @default(0)
  notes        String?
  recordedBy   User?                @relation("StaffAttendancesRecorded", fields: [recordedById], references: [id], onDelete: SetNull)
  recordedById String?
  createdAt    DateTime             @default(now())
  updatedAt    DateTime             @updatedAt

  @@unique([userId, date])
  @@index([centerId, date])
}

model TimeOffRequest {
  id           String        @id @default(uuid())
  user         User          @relation("TimeOffRequests", fields: [userId], references: [id], onDelete: Cascade)
  userId       String
  center       Center        @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId     String
  type         String        @default("PTO")
  startDate    DateTime
  endDate      DateTime
  reason       String?
  status       TimeOffStatus @default(PENDING)
  coverageName String?
  reviewedBy   User?         @relation("TimeOffReviewed", fields: [reviewedById], references: [id], onDelete: SetNull)
  reviewedById String?
  reviewedAt   DateTime?
  reviewNotes  String?
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt

  @@index([userId, status])
  @@index([centerId, startDate])
}

model TrainingLog {
  id                 String   @id @default(uuid())
  user               User     @relation("TrainingLogs", fields: [userId], references: [id], onDelete: Cascade)
  userId             String
  center             Center   @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId           String
  topic              String
  description        String?
  performedBy        String?
  hours              Float
  date               DateTime
  category           String   @default("Other")
  certificateUrl     String?
  certificateFileName String?
  recordedBy         User?    @relation("TrainingLogsRecorded", fields: [recordedById], references: [id], onDelete: SetNull)
  recordedById       String?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@index([userId, date])
  @@index([centerId, date])
}

model ClassroomBudget {
  id              String         @id @default(uuid())
  classRoom       ClassRoom      @relation(fields: [classRoomId], references: [id], onDelete: Cascade)
  classRoomId     String
  center          Center         @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId        String
  month           String
  allocatedAmount Float          @default(0)
  notes           String?
  expenses        BudgetExpense[]
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt

  @@unique([classRoomId, month])
  @@index([centerId, month])
}

model BudgetExpense {
  id              String          @id @default(uuid())
  budget          ClassroomBudget @relation(fields: [budgetId], references: [id], onDelete: Cascade)
  budgetId        String
  description     String
  amount          Float
  date            DateTime        @default(now())
  category        String          @default("Other")
  receipt         String?
  receiptFileName String?
  recordedBy      User?           @relation("BudgetExpensesRecorded", fields: [recordedById], references: [id], onDelete: SetNull)
  recordedById    String?
  createdAt       DateTime        @default(now())

  @@index([budgetId, date])
}

model TeacherEvaluation {
  id                    String           @id @default(uuid())
  teacher               User             @relation("EvaluationsReceived", fields: [teacherId], references: [id], onDelete: Cascade)
  teacherId             String
  center                Center           @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId              String
  evaluator             User             @relation("EvaluationsGiven", fields: [evaluatorId], references: [id], onDelete: Cascade)
  evaluatorId           String
  period                String
  periodStart           DateTime?
  periodEnd             DateTime?
  status                EvaluationStatus @default(DRAFT)
  overallScore          Float?
  categories            Json?
  strengths             String?
  areasForImprovement   String?
  goals                 String?
  notes                 String?
  teacherAcknowledgedAt DateTime?
  createdAt             DateTime         @default(now())
  updatedAt             DateTime         @updatedAt

  @@index([teacherId, period])
  @@index([centerId, period])
  @@index([centerId, periodStart, periodEnd])
}

model ChildPermission {
  id               String           @id @default(uuid())
  child            Child            @relation(fields: [childId], references: [id], onDelete: Cascade)
  childId          String
  permissionType   PermissionType
  status           PermissionStatus @default(PENDING)
  grantedBy        User?            @relation("PermissionsGranted", fields: [grantedById], references: [id], onDelete: SetNull)
  grantedById      String?
  signatureName    String?
  signedAt         DateTime?
  notes            String?
  effectiveDate    DateTime         @default(now())
  expirationDate   DateTime?
  formSubmission   FormSubmission?  @relation(fields: [formSubmissionId], references: [id], onDelete: SetNull)
  formSubmissionId String?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  @@unique([childId, permissionType])
  @@index([childId, status])
}

model LessonSupply {
  id            String   @id @default(uuid())
  lesson        Lesson   @relation(fields: [lessonId], references: [id], onDelete: Cascade)
  lessonId      String
  name          String
  quantity      Int      @default(1)
  unit          String?
  estimatedCost Float?
  category      String   @default("General")
  notes         String?
  createdAt     DateTime @default(now())

  @@index([lessonId])
  @@index([category])
}

enum EventType {
  HOLIDAY
  FIELD_TRIP
  PARENT_MEETING
  STAFF_MEETING
  PROFESSIONAL_DEVELOPMENT
  SPECIAL_EVENT
  OTHER
}

model Event {
  id          String    @id @default(uuid())
  center      Center    @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  title       String
  description String?
  startDate   DateTime
  endDate     DateTime
  allDay      Boolean   @default(true)
  type        EventType @default(OTHER)
  color       String?
  createdBy   User?     @relation("EventsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  createdById String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([centerId, startDate])
  @@index([createdById])
}

model ShiftSchedule {
  id        String   @id @default(uuid())
  user      User     @relation("ShiftSchedules", fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  center    Center   @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId  String
  date      DateTime
  startTime String
  endTime   String
  position  String   @default("Teacher")
  notes     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([userId, date, startTime])
  @@index([centerId, date])
  @@index([userId, date])
}

model Affiliate {
  id              String   @id @default(uuid())
  center          Center   @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId        String
  name            String
  description     String?
  websiteUrl      String?
  contactEmail    String?
  contactPhone    String?
  logoUrl         String?
  partnershipType String   @default("General")
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([centerId, isActive])
}

model CoachObservation {
  id          String          @id @default(uuid())
  center      Center          @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  coach       User            @relation("CoachObservationsGiven", fields: [coachId], references: [id], onDelete: Cascade)
  coachId     String
  teacher     User            @relation("CoachObservationsReceived", fields: [teacherId], references: [id], onDelete: Cascade)
  teacherId   String
  type        ObservationType
  classRoomId String?
  classRoom   ClassRoom?      @relation(fields: [classRoomId], references: [id], onDelete: SetNull)
  date        DateTime        @default(now())
  duration    Int?
  score       Float?
  strengths   String?
  improvements String?
  actionItems String?
  notes       String?
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt

  @@index([coachId, date])
  @@index([teacherId, date])
  @@index([centerId, date])
}

model CoachFollowUp {
  id          String       @id @default(uuid())
  center      Center       @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  createdBy   User         @relation("CoachFollowUpsCreated", fields: [createdById], references: [id], onDelete: Cascade)
  createdById String
  assignedTo  User?        @relation("CoachFollowUpsAssigned", fields: [assignedToId], references: [id], onDelete: SetNull)
  assignedToId String?
  type        FollowUpType
  status      FollowUpStatus @default(OPEN)
  priority    Priority       @default(MEDIUM)
  title       String
  description String?
  dueDate     DateTime?
  completedAt DateTime?
  notes       String?
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  @@index([centerId, status])
  @@index([createdById, status])
  @@index([assignedToId, status])
  @@index([dueDate])
}

enum ChecklistCategory {
  OPENING
  CLOSING
  HEALTH_SAFETY
  CLEANING
  MEALS
  CLASSROOM
  OTHER
}

enum ChecklistFrequency {
  DAILY
  WEEKLY
  MONTHLY
}

model DailyChecklist {
  id          String             @id @default(uuid())
  center      Center             @relation(fields: [centerId], references: [id], onDelete: Cascade)
  centerId    String
  classRoom   ClassRoom?         @relation(fields: [classRoomId], references: [id], onDelete: SetNull)
  classRoomId String?
  assignedUser   User?           @relation("DailyChecklistAssignedUser", fields: [assignedUserId], references: [id], onDelete: SetNull)
  assignedUserId String?
  title       String
  description String?
  category    ChecklistCategory  @default(OTHER)
  frequency   ChecklistFrequency @default(DAILY)
  repeatDays  Int[]              @default([])
  monthlyDay  Int?
  active      Boolean            @default(true)
  items       DailyChecklistItem[]
  notes       DailyChecklistNote[]
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@index([centerId, category])
  @@index([centerId, classRoomId])
  @@index([assignedUserId])
}

model DailyChecklistItem {
  id          String              @id @default(uuid())
  checklist   DailyChecklist      @relation(fields: [checklistId], references: [id], onDelete: Cascade)
  checklistId String
  sortOrder   Int                 @default(0)
  title       String
  description String?
  lesson      Lesson?             @relation(fields: [lessonId], references: [id], onDelete: SetNull)
  lessonId    String?
  policyDocument   PolicyDocument? @relation(fields: [policyDocumentId], references: [id], onDelete: SetNull)
  policyDocumentId String?
  policyLink  String?
  mediaLink   String?
  directLink  String?
  directLinkLabel String?
  taskTime    String?
  completions DailyChecklistCompletion[]
  createdAt   DateTime            @default(now())

  @@index([checklistId, sortOrder])
  @@index([checklistId, taskTime])
  @@index([lessonId])
  @@index([policyDocumentId])
}

model DailyChecklistCompletion {
  id          String             @id @default(uuid())
  item        DailyChecklistItem @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId      String
  completedBy User               @relation("DailyChecklistCompletedBy", fields: [completedById], references: [id], onDelete: Cascade)
  completedById String
  date        DateTime           @db.Date
  notes       String?
  completedAt DateTime           @default(now())

  @@unique([itemId, completedById, date])
  @@index([itemId, date])
  @@index([completedById, date])
}

model DailyChecklistNote {
  id          String         @id @default(uuid())
  checklist   DailyChecklist @relation(fields: [checklistId], references: [id], onDelete: Cascade)
  checklistId String
  createdBy   User           @relation("DailyChecklistNotesCreatedBy", fields: [createdById], references: [id], onDelete: Cascade)
  createdById String
  date        DateTime       @db.Date
  notes       String
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt

  @@unique([checklistId, createdById, date])
  @@index([checklistId, date])
  @@index([createdById, date])
}
